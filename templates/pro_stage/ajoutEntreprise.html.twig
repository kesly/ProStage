{%extends 'base.html.twig' %}

{%block titrePage%}
Ajouter une entreprise
{%endblock%}

{%block contenuPage%}

{{form_start(formEntreprise)}}
  {{form_row(formEntreprise.nom)}}
  {{form_row(formEntreprise.activite)}}
  {{form_row(formEntreprise.adresse)}}
{{form_end(formEntreprise)}}
{%endblock%}
