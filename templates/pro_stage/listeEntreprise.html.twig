{% extends 'base.html.twig'%}

{% block titrePage %}
Liste des entreprises
{%endblock%}

{% block contenuPage %}

<ul class="list-group">
{% for entreprise in entreprises %}
  <li class="list-group-item d-flex justify-content-between align-items-center">
   <a href="{{path('pro_stage_stages_entreprise', {id: entreprise.id})}}">{{entreprise.nom}}</a>
    <span class="badge badge-primary badge-pill">14 </span>
  </li>
   <a href="{{path('pro_stage_modifier_entreprise', {id: entreprise.id})}}">modifier</a>
{% endfor %}
</ul>

{% endblock %}
